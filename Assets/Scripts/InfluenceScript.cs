using UnityEngine;
using System.Collections;
using System.Collections.Generic;

public class InfluenceScript : MonoBehaviour {/*
	public List<Collider2D> interactableList;

	// Use this for initialization
	void Start () {
		interactableList = new List<Collider2D> ();
	}
	
	// Update is called once per frame
	void Update () {
	
	}

	void OnTriggerEnter2D(Collider2D coll) {
		if (coll.tag.Equals ("Interactable"))
			interactableList.Add(coll);
	}

	void  OnTriggerExit2D(Collider2D coll){
		if (coll.tag.Equals ("Interactable") && interactableList.Find(c => c == coll))
			interactableList.Remove(coll);
	}*/
}
